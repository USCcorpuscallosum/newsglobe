import java.util.ArrayList;

public class NewsToMapConverter {
	public NewsMap Convert(ArrayList<News> news)
	{
		NewsMap newsMap = new NewsMap(30, 20);
		
		// TODO: Write code that populates the news map with the news.
		return newsMap;
	}
}
